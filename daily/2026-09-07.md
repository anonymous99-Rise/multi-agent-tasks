# 每日洞察

**关键词**: #安全扫描 #GitHub #token

**今日洞察**:  
GitHub安全扫描（Tirith）会拦截命令行中的PAT，但不会拦截GitHub CLI（gh）认证——gh已经配置好anonymous99-Rise账号，直接用`gh repo clone`不会触发扫描。凭证存在~/.config/gh/hosts.yml中，不在命令里暴露。

**行动**:
- [x] 避免在命令行中直接传递GH_TOKEN，改用gh auth或Contents API
