{
  "projects": [
    {
      "path": "dashboard",
      "settings": {
        "buildCommand": "npm run build",
        "installCommand": "npm install",
        "outputDirectory": ".next"
      }
    }
  ]
}
